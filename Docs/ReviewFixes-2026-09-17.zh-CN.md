# 2026-09-17 审查问题修复记录

本记录对应 CodeReview-2026-09-17.zh-CN.md 的 R1–R7。原审查报告保留为历史证据。

| 问题 | 本轮处理 | 验收方式 |
| --- | --- | --- |
| R1 规则误判 | 精确区分规则类型；支持 IPv4/IPv6 CIDR；GeoIP、GeoSite、RULE-SET、源地址等需要额外上下文的规则返回无法判定，禁止跳过后宣称确定；校验 URL/IPv6 输入 | 生产规则模型 XCTest，覆盖未知规则、源/目的地址、IPv6、URL、地址族 |
| R2 规则截断 | 把总规则数传给求值器；未命中且还有未显示规则时返回无法判定；移除默认直连断言 | 499/500/501 条边界测试 |
| R3 切换状态和取消 | 明确先停后连、重置连接计时；切换期显示连接中；允许取消；提取 NetworkEngineReconnectCoordinator，在暂停点检查取消，取消不触发回滚；旧引擎未退出不得启动目标 | 真实协调器的停止顺序、取消、回滚和超时回归，以及 App/两扩展完整构建 |
| R4 授权过期 | 保存带到期时间的授权决定；连接准入每次检查时间；到期任务撤权；刷新仅对明确临时错误保留未过期授权；已验证限制即使持久化失败也先应用 | 注入时钟及真实 Ed25519 签名的控制器测试：离线宽限、到期拒绝、过期刷新、跨睡眠时间重验；Kit 准入测试 |
| R5 旧 framework 依赖 | 测速 runner 直接从当前 ProxyLatencyIndex.swift 构建；相关公开测量模型迁入同文件；不回退历史 build 目录 | 当前源码独立编译运行，无预建 framework |
| R6 网络验收假阳性 | 要求明确配置仅代理可达 HTTPS canary；拒绝可直连探针；添加/移除 alias 各自验证当前 PID/时间范围内链路事件及核心重置；失败清理 alias；不再称作默认出口切换 | 执行真实 shell gate 的模拟 SSH 回归：成功、缺事件、缺 reset、缺移除阶段证据、可直连、已有 alias |
| R7 CI 覆盖 | PR/main 自动触发；增加构建双 Rust 核心、App/两扩展、三个 Swift 测试包的独立任务；提取 test_product_build.sh 复用产品构建/单测入口 | 本地完整构建/单测；工作流配置检查。远端 CI 结果需推送后产生 |

清理了无调用的 AetherSurface/AetherElevation、ProfileInspectionHeader/CountBadge、RustCoreLinkProbe、未接入的 DiagnosticLogArchive，以及旧 App 更新状态和下载 UI 辅助代码。保留实际使用的 AR1 支持诊断、Sparkle、授权客户端、Go 服务、协议目录和测试存储实现。

规则工具定位为基础目标规则预览，策略组不是最终节点；完整运行时路由解释需要核心侧只读查询，界面现已明确限制。TUN 域名绕过仍不支持，CIDR 绕过保留。真实物理睡眠、真实默认出口切换及长期稳定性属于独立环境验收，本轮不将模拟测试计作此类证明，也不声称重新验证历史公证包。

架构文档已更新切换语义；历史功能矩阵新增状态说明；1.0.12 发布说明中与实现相反的切换顺序已更正。

工作区同期另有测速和 FlowCoreEngine 修改，本轮提交不包含这些外部修改；最终验证使用只包含本轮修改的隔离源码快照。

## 验证结果

最终结果在完成隔离快照验证后记录于下方。
