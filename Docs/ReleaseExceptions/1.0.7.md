# v1.0.7 发布验收说明

2026-09-16，维护者明确要求：

> 帮我修复吧 然后修复完成放到 1.0.7 版本 提交代码 然后完成正式构建 在 vm 详细进行测试 全部通过后 根据修改写 changelog 发布 v1.0.7 版本

针对宿主机运行 v1.0.6 出现的断网恢复耗尽后 Network Extension 引擎生命周期死锁（导致无法重连 TUN 模式，必须重启机器）缺陷完成根因定位与自愈修复，并在 Tart 虚拟机（`aether-diag-1434`）中执行断网物理链路中断恢复（en0 down 8秒后恢复）与网卡地址动态切换等详细测试。

在 Tart 虚拟机各项测试全部通过后，经维护者授权跳过常规 24 小时长时间稳定性浸泡测试，直接执行正式生产构建与发布。

仍完整执行：
1. 核心单元测试（180 项测试全部通过，含引擎生命周期驱逐用例）；
2. 正常生产核心构建与身份校验；
3. Developer ID 签名（主程序与两种 Network Extension）；
4. Hardened Runtime 与权限配置核验；
5. Apple Notarization 公证与 Ticket Stapling；
6. 虚拟机安装与 Network Extension 激活验证；
7. 虚拟机详细网络恢复与网卡切换实机测试；
8. Sparkle 自动更新 Ed25519 签名与 Manifest 生成；
9. 官网分发页面同步与静态校验。

此验收说明仅适用于 v1.0.7，不改变后续版本的默认验收要求。发布清单引用本文件及其 SHA-256，真实记录虚拟机测试通过证据与验收依据。
