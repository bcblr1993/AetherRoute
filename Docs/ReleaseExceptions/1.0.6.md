# v1.0.6 发布验收授权

2026-09-15，维护者先明确授权本次跳过 24 小时稳定性验收，随后进一步要求：

> 检查提交 以及 changelog 帮我直接 发布 v1.0.6 版本吧 正式的 苹果签名的 网站同步更新 不用在跑测试了 我之前跑了一轮

本次发布依据维护者已完成一轮测试的声明，不再运行新的功能、VM、性能、签名 Network Extension 运行时或安装后验收，也不要求补齐先前未完成的测试。24 小时稳定性验收按此前授权跳过。上述项目不记录为本轮全部通过；已有失败、忽略和中断记录保留。

仍执行最终提交与更新日志核对、正式 stable 构建、正常生产核心身份校验、Developer ID 签名（应用及两种 Network Extension）、应用与 DMG 公证及装订、签名完整性、安装包校验和、Sparkle 更新签名、GitHub 正式发布与官网同步核验。

此授权仅适用于 v1.0.6，不改变后续版本的默认验收要求。发布清单须引用本文件及其 SHA-256，明确验证依据为维护者授权和已有测试，不生成虚假通过证据。
