# Production release gates

Current product boundary: one independently distributed arm64 `AetherRoute`
app embedding both Transparent Proxy and Packet Tunnel. Local and remote
loopback/in-memory tests are regression evidence; they are not proof of a
signed real Network Extension or a notarized production release.

| Gate | Required evidence | Current state |
| --- | --- | --- |
| arm64 product build | Clean host plus both embedded extensions on two M-series Macs | Local Debug test build and a clean optimized Release product build pass warnings as errors; the combined product contains 13 inspected Mach-O files and every one is arm64-only. The Developer ID release pipeline rejects any non-arm64 slice anywhere inside the app before DMG creation. The fail-closed LAN controller has also passed in full mode on the designated second M-series Mac: staged payload and exact source manifests matched, the complete normal and sanitizer gates exited zero, host network-control state was byte-identical before and after, and the exact remote temporary directory was removed. A final repeat remains required after source freeze |
| Native unit tests | Configuration, native nodes, import, security, routing, providers, bridges | The latest complete local normal regression passed with zero failures across AetherRouteKit, Swift Testing, Transparent Proxy support, and the strong-linked FlowCore bridge. Current-source ThreadSanitizer and AddressSanitizer plus UndefinedBehaviorSanitizer runs also pass all three non-UI test bundles; the runner disables Xcode's duplicate debug-dylib executor so sanitizer builds no longer emit duplicate-rpath warnings. The designated second M-series Mac independently passes the same full normal and sanitizer gate; a final exact-source repeat remains required after source freeze |
| Live subscription import | Authorized HTTPS fetch, bounded normalization, and both-core startup without changing host networking | The supplied 46-node x2cloud export passes normalization plus FlowOnly and Packet/TUN startup with networking denied. A fresh authorized isolated check on 2026-08-02 still received HTTP 403 before any payload reached the parser. The temporary fetch directory was removed, no verifier process remained, and the local proxy/DNS/default-route/interface fingerprint stayed at its prior value. No live payload has been received or claimed as validated; the gate needs a newly issued URL or an exported payload |
| Core lifecycle | Repeated create/start/ready/stop/destroy with bounded memory | 500 PacketFlow cycles stay below the 32 MiB RSS budget; 500 FlowOnly cycles, two engines, three loopback flows, and six callbacks pass |
| Protocol matrix | Independent handshake/TCP/UDP/reconnect evidence for every catalog claim | The checked matrix covers 12 catalog protocols and 21 transport cases plus VLESS REALITY, WireGuard, ShadowQUIC, and SSH runners. On 2026-08-01 the source-build matrix passed two complete cold-start/restart cycles against pinned sing-box 1.13.15 and official ShadowTLS 0.2.25; REALITY, WireGuard, ShadowQUIC, and OpenSSH each passed their two-cycle independent gates. SSH used an encrypted inline Ed25519 private key, its passphrase, and strict host-key verification. All protocol payload peers and listeners were loopback-only; ShadowTLS additionally performed its strict certificate camouflage handshake with the configured public TLS cover host. No system proxy, route, DNS, or TUN setting was changed. All 12 typed native node variants also compile and start on both core surfaces under a network-denied sandbox; signed-provider and long-stability gates remain open |
| TUN correctness | Signed IPv4/IPv6, DNS, routing, leak, bypass, fail-closed behavior | Static settings, DNS policy ABI, UI, and in-memory lifecycle pass. The signed gate now validates both embedded extension identifiers, grants, signatures, and the Packet-only listener entitlement, then requires an owner HTTPS canary to transition from unavailable to exact SHA-256 match and back to unavailable across every TUN cycle. Organization-signed execution and dedicated IPv4/IPv6/DNS leak observations remain pending |
| Local HTTP/SOCKS5 proxy | Imported listeners removed; default off; loopback-only bind; HTTP CONNECT and SOCKS5 payload round trips; no system proxy mutation | The fixed-width host policy, fail-closed persistence/codec tests, core hardening, and random-high-port loopback black-box gate pass. The gate confirms imported HTTP/SOCKS/mixed listeners stay closed and both explicit listeners tunnel echo data on `127.0.0.1`; organization-signed Packet Tunnel runtime and refreshed UI evidence remain pending |
| Transparent Proxy correctness | Signed recursion prevention, TCP/UDP, DNS/routing, fail-closed behavior | Adapter, bridge, metadata, and loopback lifecycle pass. The signed gate now selects Transparent Proxy separately from TUN and applies the same proxy-only HTTPS canary transition on every readiness/disconnect cycle; organization-signed execution, recursion, UDP, and dedicated DNS leak evidence remain pending |
| Privacy and diagnostics | Bounded schemas and no secret/address leakage | `ART1` telemetry and `AR1` diagnostics are bounded and tested; signed provider retrieval remains pending |
| UI quality | Light/dark, English/Chinese, keyboard, resize, Reduce Motion, Increased Contrast, accessibility audit, screenshots | The Automation-free, NetworkExtension-disabled 30-image matrix in `outputs/aetherroute-ui-review-v40-final` binds product/UI source revision `5fed9829…` and has been manually reviewed across all six primary pages, all seven Settings pages, both languages and appearances, both engine selectors, privacy onboarding, and expanded text at the minimum window. The capture runner builds and launches only from a disposable source/DerivedData/HOME workspace, selects the separate Settings scene by geometry, preflights Screen Recording, SHA-256 binds every named PNG, corresponding runtime log, and complete source manifest, rejects source changes during capture plus crash/assert/precondition and AppKit table-reentrancy warnings, and deletes raw build data. The Licenses page was moved off the warning-producing nested AppKit table, its clipping was corrected, and its search/filter/selection semantics now have XCUITest coverage. Developer Tools Security is enabled. On 2026-08-07, the current isolated non-UI user-journey regression passed 303 tests with zero failures or skips; the new Network Extension approval flow reached both languages and every action, and completed the English accessibility audit. The Chinese audit was interrupted only because Chrome took the foreground, so it is not recorded as a product pass or failure. The UI runner now verifies its temporary signatures before launch and terminates, unregisters, and deletes only its exact temporary products. Final full-suite execution still requires an exclusive foreground session; final recapture, live VoiceOver, and signed Network Extension UI review remain open |
| Stability | Sleep/wake, network changes, crash recovery, 24-hour soak | Short bounded soak gates pass locally and on the designated remote M-series Mac with peak RSS below 13 MiB. The first 24-hour attempt did not pass: it completed 404 exact Flow/Packet pairs (202,000 and 404,000 lifecycles respectively) in 13,279 seconds, then FlowOnly round 405 returned the harness's UDP callback status 15 after about 5.1 seconds; there is deliberately no forged `result.txt`. Packet stayed below 12.2 MiB. A separate hashed 1,000-round reproduction passed 500,000 FlowOnly lifecycles with 9,502,720-byte peak RSS, so no deterministic round-405 threshold was reproduced. The later schema-1 runner ended without a recoverable verifier-backed result and cannot be release evidence. The current runner, harnesses, and artifacts separately passed a 75-second schema-2 release-shaped smoke over two complete 500/1,000-cycle Flow/Packet pairs, with 9,306,112/12,124,160-byte peak RSS, 2/0 peak FD growth, no new exact-core diagnostic report, and no orphan harness process. The detached current-schema 24-hour run started at 2026-08-02T02:46:16Z on the designated M-series Mac; the latest reviewed snapshot had 100 complete pairs, no failure marker, and 76 GiB free. It remains provisional until completion, final network/source checks, and verifier output. Signed sleep/wake, path-change, crash, and real-network soak gates remain open |
| Performance | CPU/RSS/throughput/latency budgets and regression comparison | The isolated-result verifier enforces FlowOnly ≤10 seconds per 500 lifecycles, PacketFlow ≤90 seconds per configured lifecycle batch, plus the recorded 64/32 MiB RSS limits. A 46-node x2cloud fixture exposed unbounded URL-test concurrency; the shared eight-permit budget and 64-task regression prevent descriptor exhaustion. The retained current unsigned arm64 TCP gate passes at 3,612.962 MiB/s FlowOnly and 3,127.443 MiB/s PacketFlow loopback SOCKS5, with 0.024/0.031 ms added p95 and raw socket ratios retained. A full fallback-`sample` disconnected-idle diagnostic passed two five-minute warm-ups plus two ten-minute samples with 0% median/p95 CPU, 124,928,000/127,205,376-byte maximum RSS, no app network sockets, and unchanged system proxy state. It is retained as revision-bound provisional evidence, not final `xctrace` release proof. Signed Network Extension CPU, throughput, latency, sleep, and path-change evidence remains open |
| Brand and App Icon | Current Apple system renditions, small-size legibility, deterministic source, product integration | The redesigned layered `AppIcon.icon` exports Default, Dark, Mono, Tinted Light/Dark, and Clear Light/Dark at 16, 32, and 1024 px through Apple's current Icon Composer renderer. Portal and route masks plus the macOS 15+ fallback asset catalog regenerate deterministically; an arm64 application build succeeds and emits both `AppIcon.icns` and `Assets.car`. The v40 overview and About captures show the matching current SwiftUI brand glyph and application icon in both appearance families; final frozen-source recapture remains part of the UI gate |
| Licenses and metadata | Complete deterministic packaged notice and exact release metadata | One combined 438-component notice passes source and built verification. Fresh locked/offline normal-dependency scans of both `aether-flow-only` and `aether-embedded` release graphs report zero GPL/AGPL/LGPL/MPL runtime records, zero externally unlicensed records, and no `tuic-core` or Mihomo runtime component; ShadowQUIC's linked crates report MIT. Stable timestamp/version and final legal metadata remain external inputs |
| Developer ID release | Exact signed app/DMG, hardened runtime, timestamps, notarization, staple, Gatekeeper, manifest | The fail-closed candidate pipeline requires both exact current-tree dual-engine Apple Development runtime evidence and a complete schema-2 24-hour soak whose runner, harnesses, and both core hashes match the release tree. Signed runtime evidence requires at least three TUN and three Transparent Proxy canary-transition cycles and retains neither the canary URL nor raw xcresult. Long evidence requires at least 800 paired rounds, consistent UTC boundaries, production RSS/FD budgets, and at most 1 MiB/hour post-warm-up RSS growth. The candidate manifest binds both evidence digests and is explicitly marked `notarized-candidate`; it is not a production update manifest. The pipeline then records the complete source manifest, reruns normal plus direct-`xctest` TSan and ASan+UBSan gates in disposable DerivedData, and rejects source changes before archive creation. Production promotion remains blocked on exact-DMG installed runtime, leak, sleep/wake, path-change, crash-recovery, and clean-machine gates. The signing machine now has a valid Developer ID Application identity, all three explicit Developer ID profiles, and a working notary Keychain profile; Apple accepted and the DMG stapler validated the `2026080703` test candidate. That candidate predates later source fixes and is not production evidence. All three current profiles omit the required registered `group.com.aetherroute.desktop` App Group grant, so the strict preflight now rejects them until all three App IDs are assigned to that group and the profiles are regenerated. Real inputs must remain outside source control |
| Installation and update | Clean-machine install, upgrade, rollback, license/update service failure behavior | Signed receipt/manifest clients, Keychain persistence, localized account UI, 24-hour refresh, and a host-level connection gate pass. The deployable Go service stores no plaintext activation key, separates the receipt signer, refuses non-loopback listeners, passes race tests and static Linux arm64 compilation, and interoperates with the Swift client across activate, refresh, signed update, deactivate, and fail-closed paths. Verified DMG download rejects redirects/size/hash failures and atomically commits only matching bytes; its SHA-256 verification, staging copy, and atomic replacement run in a detached task so large artifacts cannot block the main actor. A TaskLocal regression and the release static pipeline pin that boundary. A temporary-root drill passes arm64 Release-DMG install, upgrade, rollback, and encrypted-profile preservation without writing `/Applications` or launching the app. The promotion verifier requires the exact candidate DMG plus installed Developer ID dual-engine, IPv4/IPv6/DNS leak, recovery, CPU/RSS, throughput/latency, UI responsiveness, and clean-machine evidence before producing a separate `production-approved` manifest; it performs no upload. Real post-install evidence, payment/customer integration, hosted HTTPS staging, and signer/state backup drills remain open |

## External inputs that must never be committed

- Apple Developer Team ID, final bundle identifiers, App Group, and Keychain
  access group
- Developer ID Application certificate installed in the signing Keychain
- matching host, Transparent Proxy, and Packet Tunnel provisioning profiles
- a locally stored `notarytool` Keychain profile
- final version/build/release time, copyright holder, support/privacy URLs
- licensing and update service base URL, API contract, product identifier, and
  public verification key

Do not send private keys, certificate export passwords, Apple ID passwords,
two-factor codes, or notary credentials through chat. Configure them directly
on the designated signing machine.
